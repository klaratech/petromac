name: Build Flipbook Assets

on:
  push:
    branches:
      - main
    paths:
      - "assets/tags/**"
      - "scripts/build_flipbook.py"
      - "scripts/update_flipbooks.py"
      - "scripts/node/validate-flipbooks.ts"

jobs:
  flipbook:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y poppler-utils

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/python/requirements.txt

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Enable pnpm
        run: corepack enable

      - name: Install Node.js dependencies
        run: pnpm install --frozen-lockfile

      - name: Check for source PDFs
        id: source_pdfs
        run: |
          if [ -f assets/source-pdfs/success-stories.pdf ] && [ -f assets/source-pdfs/catalog.pdf ]; then
            echo "present=true" >> $GITHUB_OUTPUT
          else
            echo "present=false" >> $GITHUB_OUTPUT
          fi

      - name: Build flipbook assets
        if: steps.source_pdfs.outputs.present == 'true'
        run: pnpm run build:flipbooks

      - name: Validate flipbook assets
        run: pnpm run validate:flipbooks

      - name: Commit and push generated assets
        if: steps.source_pdfs.outputs.present == 'true'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/flipbooks/
          git diff --quiet && git diff --staged --quiet || git commit -m "chore: update flipbook assets"
          git push
