# Petromac Website

Next.js 16 (App Router) + React 19 + TypeScript website with public site, intranet, and trade-show kiosk.

## Quick Reference

- **Dev**: `pnpm run dev` (http://localhost:3000)
- **Lint**: `pnpm run lint`
- **Typecheck**: `pnpm run typecheck`
- **Build**: `pnpm run build` (also runs automatically on `git push` via husky)
- **Unit tests**: `pnpm run test:unit` (node:test via tsx, all `src/**/*.test.ts`; runs in CI and pre-push)
- **E2E tests**: `pnpm run test:e2e` — Playwright starts its own `next start` (build first), so no separate server step. Runs in CI as its own `e2e` job. Point it at a deployed environment with `PLAYWRIGHT_BASE_URL=https://test.petromac.co.nz`. Reuses an already-running dev server locally.
- **Data pipeline**: `pnpm run data` (rebuilds operations JSON + flipbooks)
- **HTML catalog**: `pnpm run data:catalog` (rebuilds `catalog.json` + web images from the InDesign IDML in `sources/catalog/` — see docs/ADMIN.md §2b)
- **Patent PDFs**: `pnpm run data:patents` (dry-run) / `--apply` — shrinks new granted-patent scans. NOT the `/ebook` recipe the catalog uses; that makes these files bigger. See docs/ADMIN.md §3.
- **Validate**: `pnpm run validate:flipbooks && pnpm run validate:successstories`

## Commit & Push

Git hooks (husky + lint-staged) gate commits and pushes. `pnpm install` activates them via the `prepare` script.

- **pre-commit** runs `lint-staged` on whatever's staged: prettier on every text file, plus `eslint --fix` on `.ts/.tsx/.js/.jsx/.mjs/.cjs`. Fast — a few seconds. Files that get autofixed are re-staged before the commit lands. Unfixable eslint errors abort the commit.
- **pre-push** runs `pnpm typecheck && pnpm lint && pnpm test:unit && pnpm build` in that order — cheap checks first, ~30–60 s build last. Catches Next.js build-time issues that tsc/eslint miss (server/client component boundary mistakes, bad dynamic-import paths, route conflicts, production-only codepaths). The deploy build won't fail if pre-push passed.

Escape hatch when you really need it: `git commit --no-verify` or `git push --no-verify`. Use sparingly and only when you've verified separately — bypassing pre-push is how the deploy build catches you.

Workflow we use: stage with `git add`, commit, push. Don't run `pnpm build` manually before push — the hook does it. Don't run `prettier --write` manually on a whole tree before commit — lint-staged scopes it to changed files for you.

**For Claude:** when suggesting a commit or push, ALWAYS give the user one ready-to-paste shell command — full `git add <files> && git commit -m "..." && git push`, with the file list spelled out and the commit message inline. No placeholder fragments, no "then run git commit" prose split across paragraphs. Group everything in a single fenced block so it's one copy-paste action. When in doubt about which files to stage, prefer `git add -A` (or list every modified path explicitly) over leaving the user to figure it out.

**Deploy model (Jul 2026): pushes go to TEST, production needs the button.** Every push to main auto-deploys https://test.petromac.co.nz (noindex staging build; share freely for feedback). Production (www.petromac.co.nz) changes ONLY via the "Promote to Production" workflow — `gh workflow run deploy-prod.yml` or GitHub → Actions. When Rajesh says "go live", run the promote. See DEPLOY.md.

**Lockfile reminder:** `package.json` and `pnpm-lock.yaml` ALWAYS change together. Any `pnpm add` / `pnpm remove` / `pnpm update` produces a diff in both. If `git status` shows one without the other, that's a deploy-build trap — `pnpm install --frozen-lockfile` will fail. Stage both in the same commit. The pre-push hook does NOT catch this; only the deploy build does.

## Architecture

- **Route groups**: `src/app/(public)/` for public site, `src/app/(kiosk)/` for kiosk shell
- **Intranet**: `/intranet/*` is the staff area; Microsoft sign-in can be enabled for staff identity
- **Shared components**: `src/components/shared/` (header, footer, flipbook)
- **Map components**: `src/components/geo/` — `DrilldownMapCore` is the shared map; the public `/track-record` page (map-as-hero, iterated Jul 2026) opens directly with the map card — no header band. The card's own header row holds the H1, the system filter chips (moved out of the in-map bottom bar via the core's `selectedSystems`/`hideSystemFilter` controlled-mode props; kiosk surfaces keep the built-in bar), and a quiet "Records & success stories ↓" anchor. The live deployments counter + cumulative sparkline live in ONE compact overlay card top-right inside the map (the old legend position; SSR default "3,118+", filtered subsets show exact counts; pointer-transparent; hides while a country's yearly-stats drawer is open via `onSelectedCountryChange`). Filter state lives once in `TrackRecordExperience` (client) and drives map + overlay — both use `cumulativeDeploymentsByYear()` in `lib/map/process.ts` (the map's exact counting semantics), with the all-systems curve baked at build. The crawler summary sentence lives in the map's pre-load placeholder. The card is `isolate` so in-map overlay z-indexes (Top 5 panel, tooltip) can't paint over the sticky header; the color legend was removed by design (hover tooltip + Top 5/Show-all carry precise values). The data fetch is versioned with `operations_stats.json`'s `generatedAt` (see `fetchOperationsData`) so all surfaces describe the same dataset generation — kiosk callers keep the bare URL for stable SW cache keys. The kiosk dashboard uses `DrilldownMapKiosk` as a wrapper
- **Feature modules**: `src/features/` (success-stories filters/services, flipbooks, catalog config/specs)
- **Catalog (three-level drill-down, Jul 2026)**: `/catalog` (Level 1) shows product-line bands — Wireline Express™ (Tool Taxis, Guides & Holefinders family cards), Focus™ (Centralisers), compact Well Intervention row — with computed product counts and group chips. Layout polished Jul 2026 into three coherent blocks: (1) H1 + intro; (2) a balanced two-column row — the "Find a product" panel on the left with the catalog PDF actions as a narrow card beside it (Download primary, Email secondary, under a "Complete catalog PDF" eyebrow); on mobile the PDF card comes FIRST so the actions stay above the fold. The PDF actions used to sit in a large card at the very BOTTOM of the page. NOTE the house spelling is "catalog", never "catalogue" — the latter crept in twice via an outside prompt. **"centraliser" and "centralizer" are DIFFERENT: both are correct and both appear in visible copy on purpose** (Rajesh's call, Jul 2026, from the SEO audit) — engineers search both spellings and the -ize form carries more US volume, whereas nobody searches for a product by the word "catalogue". Do NOT normalise the centraliser family to one spelling. The split is by page, never within a sentence or heading: -iser on the family page, the `Focus™ Centralisers` brand term, CP8/CRIL/CX9; -izer on CP12/CA7/CRU. The "Find a product" panel merges what were two disconnected cards — the search box (navigates to model pages) leads, with the Device Finder's size/purpose filters beneath a divider, a deliberately narrow `sm:w-44` FREE-TEXT size input (not a number spinner — the counter was useless for a value you tap or type, and couldn't express "8-1/2"; it parses via `enrich.ts`'s existing `parseInchValues`, so decimals, ascii fractions and print typography all work), an Open hole / Cased hole toggle that swaps the preset chips (OH = bit diameters 6/8-1/2/12-1/4/17-1/2; CH = casing ODs 7/9-5/8) and relabels the field Hole vs Casing size — it does NOT filter results, since the catalog data has no open/cased dimension, it just makes clear which number is wanted — and a "Clear filters" action that appears only when a filter is active; (3) family cards. The Device Finder remains a client island (hole/casing size + purpose → matching models; no SKU names in the rendered markup, family cards stay the no-JS path). The bottom strip is understated supporting info — patent notice + "View patents" and a quiet PDF download text link. The "Contact our regional managers" CTA that used to be the prominent action there was REMOVED Jul 2026: contact now lives in the header's mail icon, which opens `ContactDrawer` (a real form) on every page, so a page-specific CTA was redundant. `/catalog/[category]` (Level 2, SSG ×4) renders family SPEC TABLES from parsed fields: tool-taxis is ONE unified 8-model table (Role + Bearing columns); guides-holefinders features Pathfinder/HT then vendor sections (For SLB / Halliburton / Baker Hughes tools); focus-centralisers splits Open/Cased hole; well-intervention is one Accessories table. `/catalog/[category]/[slug]` (Level 3, SSG ×32, URLs unchanged) adds an "Other models in this family" table. Old `/catalog?category=X` URLs 301-redirect to the family pages. Content model: `src/features/catalog/content/catalog.json` (GENERATED from the InDesign IDML via `pnpm run data:catalog`; curation in `scripts/python/catalog_config.json` — NEVER hand-edit catalog.json) + `src/features/catalog/content/enrich.ts`, the WEBSITE layer over the generated JSON: slug-keyed curation (vendor per PDF TOC, finder purpose, taxi roles), a fraction-aware spec parser (hole/casing range, bore, temp, weight; 5 PDF-verified overrides), family summaries/table rows/finder index. New devices in a future edition get safe defaults + a build-time warning to add a curation row — a new PDF edition is just drop-IDML + regen. Email PDF sends as the signed-in staff member (`/api/staff/send-pdf`) with `info@` fallback; the compressed catalog PDF remains the download/email artifact.
- **Success Stories (Jul 2026; renamed from Case Studies INCLUDING the URL, Aug 2026)**: `/success-stories` index + 46 SSG pages at `/success-stories/[slug]` — the success-stories flipbook, one page per story. The whole Jul-era `/case-studies` tree 301s here in one hop (slugs unchanged; see redirects.ts step 5b + docs/DECISIONS.md Aug 2026). **This REPLACED the public flipbook (phase 1 of its retirement, Jul 2026)**: `/success-stories/flipbook` 301s to the index. The index is a client island (`CaseStudiesBrowser`) with free-text search + region/challenge/product/company filters over `src/features/case-studies/filters.ts` (pure, unit-tested — 25 tests). Counts are FACETED (Aug 2026): each dropdown counts against the other active filters but NOT its own, so numbers never exceed the result count while siblings stay reachable; empty combinations render `(0)` disabled rather than disappearing, and order comes from the unfiltered tally so nothing reshuffles. The Download/Email buttons share one flag + one label helper (`SHOW_FILTERED_COUNT_ON_ACTIONS`, `actionButtonLabel()`) so they can never disagree — currently OFF, flip the const for "Download 16"/"Email 16". The filtered set is still downloadable/emailable as a PDF because every story carries its flipbook `page` number — exactly what `/api/pdf/send-pdf` and `/api/pdf/success-stories` take. All 46 cards are in the SSR HTML, so crawlers and no-JS still see every story. Card + detail badges show country, product and ONE application (`categories[0]`) — the year badge was dropped Jul 2026 because an application tells a reader whether a story is about their problem and a year doesn't. `filters.ts` `categoryLabel()` strips the "Well Access: " prefix for DISPLAY only (so siblings read "Deviation"/"Ledges" while the one bare "Well Access" story keeps its name); the full value stays the filter key, otherwise that story would collapse into a sibling and lose its option. The "Well Access:Deviation" missing-space typo was fixed in `tags.csv` (Jul 2026) — but tags.csv is GENERATED from the tags xlsx in `sources/success-stories/` (gitignored, outside the repo), so the fix regresses if the xlsx still has it and gets rebuilt; `normalizeCategory()` stays as the guard. STILL PENDING (phase 2): the KIOSK keeps the flipbook — `/intranet/kiosk/successstories` and, more deeply, `LogsScreen.tsx`'s CH-lane Case Studies takeover with per-product preset filters. `Flipbook.tsx` + `SuccessStoriesFlipbook.tsx` can only be deleted after that; the flipbook INGEST pipeline stays forever, since case-studies uses its generated page webps as each story's visual. `case-studies.json` is GENERATED by `scripts/python/build_case_studies.py` from the flipbook `source.pdf` + `tags.csv` (extracted headline/C-S-R/narrative + CSV tags). **Story pages render EXTRACTED FIGURES, not the published page (Aug 2026)** — they used to show the whole flipbook page webp below the extracted prose, i.e. the same story twice, the second time as pixels a reader can't select or resize and Google can't read. `scripts/python/extract_story_figures.py` pulls just the figures into `public/flipbooks/success-stories/figures/` (+ `manifest.json` + a self-contained `REVIEW.html` for eyeballing all 46 pages); `build_case_studies.py` folds the manifest into each story as `figures[]` with captions matched from the page's own "Fig.N" lines. Run the extractor FIRST — it warns, not fails, if the manifest is missing. The published page survives as a **download** ("Download this page (PDF)" → the same `/api/pdf/success-stories` endpoint the hub uses, with a one-page list) and as the og/twitter/JSON-LD share image. Five general rules do the filtering and there are NO per-page patches (`CAPTION_OVERRIDE`/`DROP_FIGURE` are both empty — keep it that way, fix a rule instead): (1) images repeated on 3+ pages AND under 300k px are furniture (category icons) — the size qualifier is load-bearing, since a product render legitimately reused across stories about the same tool trips the repeat count, and dropping it cost page 27 both figures and pages 9/10 their captioned one; icons are ~105k, those renders 673k–1.0M; (2) the region world-map is matched by its SLOT (height exactly 337 at top≈290/left≤60) NOT its dimensions, because it is 605, 611 or 620 wide and an exact-dims test left it as a spurious Fig.1 on 18 pages, cascading every caption down by one; (3) same-slot repeat draws are deduped (page 20 paints one tile 16 times — that was "18 figures"); (4) `MIN_AREA` 60k floors out slivers — deliberately low, because page 16's real log tracks are 74k, SMALLER than the icons, so area alone can never separate the two populations; (5) gray/1-comp images with no smask row of their own are ORPHAN MASKS — alpha channels for vector art that `pdfimages` lists as type "image" and that extract as a white silhouette on solid black. Seven survived every other filter, two of them under real "Fig.N" captions. Note `pdfimages -list`'s "object ID" header is TWO columns: the object number is index 10, index 11 is the ID and is 0 on every row. Figures sort by reading order via `pdftohtml -xml` positions, since PDF object order is not reading order. **`pdftohtml` writes extracted images beside its INPUT file**, so the script reads through a symlink in `/tmp` — without that every run drops ~250 stray PNGs into `public/`; `cwd` doesn't move them and `-i` kills the `<image>` tags. KNOWN GAP: figures on pages 7, 9, 10 and 17 come out split, because the layout composites them; the InDesign IDML has the original assets and supersedes the extraction (see TODO). Slugs are FROZEN: 21 of them are the old WordPress-era slugs (kept when the PDF versions replaced the WP content, so live URLs + the root-level WP 301s in `src/lib/redirects.ts` stayed valid); new-edition pages need a NEW_SLUG entry in the script. Article+BreadcrumbList JSON-LD, all in the sitemap. Raw WP mirror archived outside the repo at `Website_Archive/oldsite-case-studies/`. **Every story page ends in three exits (Aug 2026): related stories, its catalog family, and contact.** Before that each story had exactly ONE inbound link (the hub) and ONE outbound (back to the hub) — a crawl dead end, and a large part of why 83 pages sat in "Discovered/Crawled – not indexed". `relatedCaseStudyMap()` in `filters.ts` is the source: product line is a PRIMARY SORT KEY (not a weight — as a weight, category+country overlap beat it), and a global coverage pass guarantees no story is nobody's neighbour. Do NOT simplify it to a per-page top-N sort: that was tried, and left 7 stories orphaned while popular ones took 10 links. Min inbound is now 2, and the unit tests fail if that regresses.
- **Shared UI primitives**: `src/components/ui/`
- **API routes**: `src/app/api/` (email, PDF generation). Operations and country-label data are read from static JSON in `/public/data/` directly, not via `/api/data/*` — CDN-cacheable, and every surface (incl. the offline kiosk) works without the backend. (A legacy Vercel deployment was retired Jul 2026; Hetzner behind Cloudflare is the only deploy target.)
- **Nav shape (Aug 2026)**: top bar is Home / About / Catalog / Track Record /
  Success Stories / Simulation. **About does not navigate** — it is a
  `<button>` that only opens its menu, now just **Origins → `/about`, Team**.
  **Publications AND Patents both left the About cluster** — neither is in the
  menu nor the `/about` sidebar. Both are EVIDENCE, not company background, so
  they belong with the other evidence pages rather than under "About".
  **Both keep their routes** (`/about/publications`, `/about/patents`) and
  sitemap entries — moving the paths would cost a 301 and a re-crawl for
  nothing.
  They are reached through **`SeeAlso`** (`src/components/public/SeeAlso.tsx`),
  a compact card in the page header of `/success-stories`,
  `/about/publications` and `/about/patents`. It owns the list of the four
  evidence pages (success stories, track record, publications, patents) and
  takes the CURRENT page's path, rendering the other three — so a page can
  never link to itself, the set can't drift per-page, and adding a fifth
  evidence page wires it everywhere in one edit. Hand-listed "see also" links
  are exactly how Publications ended up reachable from one place.
  **`/track-record` is deliberately NOT yet a SeeAlso host** — it has no
  conventional header (map-as-hero) and already carries a prominent Success
  Stories CTA plus a publications link in its records strip. It is the one
  asymmetry in the cluster; see TODO.
  Two traps: `isSubActive` must keep its EXACT-match special case for `/about`
  or Origins also lights up on `/about/patents` — still required even though
  Patents left the menu, because the ROUTE is still live; and every nav link (including
  the LOGO) must call `handleNavClick`, because Next ignores same-URL
  navigation and the click otherwise does nothing but leave you mid-page.

- **Working from a Cowork session**: the bridge VM is Linux but `node_modules`
  are macOS, so `pnpm typecheck/lint/test:unit/build` CANNOT run there — the
  pre-push hook is the first real gate. Commit with `--no-verify` (pre-commit
  needs pnpm), move git lock files to `_to_delete/` (the bridge cannot `rm`),
  `rm -rf .next` after any route rename, and `md5sum` scratch copies against
  the repo before trusting a test run. Traps table + the cloud-container test
  recipe: docs/DEVELOPMENT.md "Working on this repo from a Cowork session".

- **Trademark marking**: Petromac™, Wireline Express™, Focus™, Tool Taxis™,
  Thor™, Hermes™ are marked on PROMINENT USE ONLY — never in SEO titles/meta,
  `alt`/`aria-label`, JSON-LD, legal entity names, URLs or data/filter keys.
  Marks live in display-label maps (`PRODUCT_LINE_LABELS`, `FAMILY_LABELS` /
  `familyLabel()`, `SYSTEM_LABELS`) so generated data and filter keys stay
  clean. Full rule + the do-not-mark table: docs/VOCABULARY_MAP.md §0 — read it
  before changing any copy.

- **Track record — Myanmar is WITHHELD from everything published.** Rajesh's
  call (Aug 2026), implemented as `EXCLUDED_COUNTRIES` in
  `scripts/python/normalization_config.py`: the 54 Myanmar rows are dropped
  right after country normalisation, before any artifact is written, so they
  reach no map, chip, tooltip, Top-5 panel or headline number. It was briefly
  published AS Vietnam instead; that was reverted the same day because it made
  the map claim deployments in a country where they never happened. Suppression
  keeps every country the site does show truthful (Vietnam is 50, its real
  figure) at the cost of a smaller dataset: published totals sit BELOW Jobs
  History Master by exactly the excluded rows (3,507 of 3,561 records; 3,114
  deployments). That gap is the exclusion list, not a pipeline fault. The
  source workbook is never modified. Note this is NOT the France/French-Guiana
  pattern — that was a topology fault fixed in the geometry, and renaming a
  country to paper over a map bug remains wrong.

- **Kiosk**: OH/CH split — `/intranet/kiosk` (splash with Open Hole / Cased Hole buttons + a bottom-right "Prime offline" pill that routes to `/intranet/kiosk/prime`). OH → `/intranet/kiosk/lane?lane=oh` is now a pure attractor: a fullscreen subtitled-video playlist (dice intro sting + three narrated product clips) with native browser controls on the narrated clips and a small prev/next + dot strip at the bottom-centre that auto-hides 4 s after the last tap. The old right-edge product button strip (Formation Testing / High Deviation / PathFinder → `OverlayExperience`) was dropped in May 2026, and `OverlayExperience.tsx` was deleted in the June 2026 cleanup. CH → `/intranet/kiosk/ch` lands in `HelixExperience`, which is now the CH lane orchestrator with three view tiers: (1) **video** — looping Helix attractor with native controls and two bottom-right corner badges (Helix, Rocker) + a top-right ✕; (2) **product** — image-based product page for whichever badge was tapped (`HelixProductScreen` shows three side-by-side panels for CX7/CX9/CX13; `RockerProductScreen` shows Rocker + Rocker Inline); (3) **mechanism / logs** — `MechanismScreen` slides or `LogsScreen` pager, configs picked from `deviceSpecs` based on the active product. Every screen from tier 2 onward carries the same persistent top-center pill: **Mechanism · Case Studies · Specifications** (Specs renders only when `config.specs` is set; tapping it opens `SpecsModal` on top of the current screen). All ✕ from a sub-view jump straight back to the looping video — no hierarchical step through the product screen on exit. The standalone `RockerExperience.tsx` was retired and deleted. The old CH lane attractor at `/lane?lane=ch` is gone too — the URL still redirects to `/ch` for bookmarks. The legacy `/productlines` tile grid (with `SystemModal` and `featuredSystems`) was removed entirely in July 2026. Kiosk videos come straight from `public/videos/transcoded/` (1080p; the small mechanism clips remain SD). The `kiosk-hd/` tier, the `useKioskVideo` hook and the `?sd=1` flag were RETIRED Aug 2026 — they existed only while `transcoded/` was 540p, and duplicated ~170 MB once it wasn't. Launch URLs still carrying `?sd=1` are inert. The prime manifest's `optional` bucket is now just the 3D models, so every prime pulls the full video set. Native `controls` are exposed on the lane attractor (narrated clips only — dice stays chrome-free) and the Helix video; mechanism clips inside `MechanismScreen` stay bare. Track Record + Success Stories live INSIDE `Case Studies` (first slide of the pager is the drill-down map, in-map link opens Success Stories as an inline takeover).
- **Homepage (v2, Jul 2026)**: hero with a typewriter headline (full text always in SSR HTML; animation is a hydration-time enhancement, reduced-motion-aware, runs once) over a seamless 13.2s background loop (`public/videos/hero/hero-loop.mp4` via `LazyVideo` — IO-gated so mobile/reduced-motion never download it); `ChallengeSelector` tabs; matched compact Hardware/Software bands; `ProofSection` stats from the build-time stats snapshot; contact section. `AthenaTerminal` (a simulated terminal that types six lines on first view, fully server-rendered) lives on `/simulation` inside `AthenaInAction` — moved off the homepage Jul 2026 to keep the software band a compact bar.
- **SEO (Jul 2026, launch prep)**: `src/lib/seo.ts` `pageMetadata()` builds every page's canonical + per-page OG/Twitter tags (App Router replaces, not merges, `openGraph` — always go through the helper). `src/lib/siteUrl.ts` `isProductionSite()` drives indexability: any site URL other than petromac.co.nz ships meta noindex + `X-Robots-Tag` + Disallow-all robots.txt; `next.config.ts` fails the build if `NEXT_PUBLIC_ENV=production` points at a non-production domain. robots/sitemap are `app/robots.ts` / `app/sitemap.ts`, env-derived. JSON-LD: Organization+contactPoint (home), Product+BreadcrumbList (product pages), ScholarlyArticle ItemList (publications). Titles are un-branded — the root template appends "| Petromac" once, so a base title must be ≤49 chars to keep the SERP title under 60. **Sitemap `lastmod` must never fall back to build time** — every entry was `new Date()` until Aug 2026, which told Google all 94 pages changed on every deploy (a re-crawl signal pinned to "everything, just now" is the same as no signal). Dates come from the content: catalog edition date parsed from the IDML filename, the flipbook manifest's `updatedAt` for stories, `PAGE_LAST_MODIFIED` for authored pages. **`loading.tsx` is an SEO trap on a content page** — `/track-record` was the only route with one, and that Suspense boundary flushed the skeleton inside `<main>` while the real content streamed into a hidden template after the footer; deleting it put 150 words back in `<main>`. **`docs/VOCABULARY_MAP.md` is the signed-off source of truth for every page title** (14 core/category routes + all 32 product models, applied 30 Jul 2026) and for the -iser/-izer spelling policy; change a title there first. Product titles are NOT the visible H1 — they live in `PRODUCT_TITLES` in `src/features/catalog/content/enrich.ts`, an SEO-only map, so editing one changes `<title>` and nothing on the page. Two JSON-LD notes from the 30 Jul Search Console pass: case-study `Article.about` must be `Thing`, not `Product` (a case study is about a tool, it doesn't offer one), and product pages carry NO `offers` block — the placeholder `$0.00` one was **removed 31 Jul 2026** because a price turns a Product into a _merchant listing_ in Google's eyes, and publishing "USD 0.00 / InStock" for made-to-order equipment is a data-quality violation. **Consequence, and it is expected: Search Console reports "Product snippets: 1 invalid item" on every product page** (seen 10 Aug 2026 once the catalog URLs were indexed; family pages are clean since they emit only BreadcrumbList). `offers` is recommended, not required — the warning is accepted on purpose and is permanent unless real prices or reviews exist. **Do not clear it by re-adding offers**; that exact fix was tried on 30 Jul and made things worse. The reasoning is in a comment block above `productSchema` in `catalog/[category]/[slug]/page.tsx`.
- **Redirects / URL migration (Jul 2026)**: EVERY redirect lives in `src/lib/redirects.ts` — a pure, unit-tested mapping table — applied by `src/proxy.ts`. NOT in `next.config.ts` `redirects()`, and don't put one back there: Next normalises the trailing slash BEFORE consulting that table, so an indexed WordPress URL like `/contacts/` became `/contacts` first and either chained (308 → 301) or 404ed outright. The 30 Jul 2026 Search Console audit found six such pages dead, `/contacts/` among them at 59 clicks / 1,045 impressions. So `next.config.ts` sets `skipTrailingSlashRedirect: true` and the proxy owns slash canonicalisation — that's what makes `/legacy/` and `/legacy` BOTH single-hop 301s. The proxy must therefore keep emitting the ordinary `/page/` → `/page` 308; delete that and every page grows a duplicate URL. Legacy hops are literal 301 (not Next's `permanent: true` 308). Dead WP feed URLs → 410. Unrecognised query strings on public URLs → 404 (Google had crawled `/?11667727895.html` and friends as separate 200 pages); `utm_*`/`gclid`/`fbclid` etc. stay allowed, and `ROUTE_QUERY_PARAMS` is the extension point — **a page that starts reading `searchParams` without registering the param there will 404**. `PATENT_PDF_RENAMES` covers the four patent PDFs whose filenames had spaces/commas; the test fails the build if a new upload needs percent-encoding.
- **Hosting & DNS (migrated 10 Aug 2026)**: the site is served from **klaratech-1** (Hetzner) through a **Cloudflare Tunnel** in the **company** Cloudflare account `it@petromac.co.nz` — not Rajesh's personal account, which is where it lived until 10 Aug. Nameservers are mina/rudy (was carol/harley; a zone move between accounts reassigns them). klaratech-1 now runs **TWO** cloudflared units: the original `cloudflared.service`, which also fronts four other domains and must keep running, and `cloudflared-petromac.service` for the new tunnel. **Never run `cloudflared service install` on that box** — it overwrites the original unit. The tunnel's three DNS records must stay **PROXIED**: a `cfargotunnel.com` hostname has no public A record, so DNS-only takes the site down completely. Full detail: docs/DNS.md + DEPLOY.md.
- **Analytics (Jul 2026)**: Cloudflare Web Analytics via `WebAnalytics.tsx` in the root layout — cookieless, so the site runs with NO cookie consent banner (browsing sets no cookies at all; the only cookies are staff-auth ones after an intranet sign-in). Gated on `NEXT_PUBLIC_CF_ANALYTICS_TOKEN` AND `isProductionSite()`, so test traffic never reports. The analytics token was re-issued in the company account on 10 Aug 2026 (only the GitHub repo variable changed — the Dockerfile ARG and deploy-prod build-arg already existed; historical data does not migrate between accounts). NOTE any `NEXT_PUBLIC_*` var needs THREE things or the deploy silently no-ops: the GitHub repo variable, `ARG`/`ENV` in `Dockerfile`, and a `build-args` entry in `deploy-prod.yml`. The CSP in `next.config.ts` must also allow any new third-party host or the script is blocked silently.
- **Styling**: Tailwind CSS 4. CRITICAL: `src/app/globals.css` must keep the `@config '../../tailwind.config.ts'` directive — v4 does not auto-load a JS/TS config, and without it every config-derived utility (brand palette, `font-heading`, `shadow-card`, type scale) silently compiles to nothing (this was broken until Jul 2026). Brand tokens (`brand`, `brandblack`, `brandgray`) live in `tailwind.config.ts`; homepage v2 components use the `slate-*` scale for body text — `brand` (#1E4A9A) remains the single brand color across the homepage and Track Record. Legacy `brandblack`/`brandgray` are still in use on error pages, the team page, and intranet surfaces. Fonts: Inter (body), IBM Plex Sans (headings). Scroll feel is CSS-only (globals.css): reduced-motion-aware smooth anchor scrolling, `.scroll-reveal` settle-in on home/simulation sections (scroll-driven animation, no snap anywhere by design), `.header-elevate` shadow, `:where(section[id])` scroll margins, `.caret-blink` for the typewriter carets.

## Data Organization

- `public/data/` — Published JSON/CSV served via CDN (fetch at runtime, never import). Operations data ships as three artifacts: `operations_data.json` (slim, 6 columns: Country / System / Subsystem / Year / Successful / PathFinder Run (Y/N) — what every map surface reads, ~600 KB), `operations_full.json` (all 33 columns from the source xlsx, ~3.5 MB, only fetched by the staff diagnostic at `/intranet/kiosk/datacheck`), and `operations_stats.json` (tiny headline numbers the homepage ProofSection and track-record page import at build time — the homepage never fetches the big file; the track-record page ALSO imports the slim file server-side at build for its cumulative growth chart, which ships as static SVG). Full schema is documented at the top of `src/types/JobRecord.ts`. Add a column to the slim file when a new filter or display needs it.
- `public/flipbooks/` — Generated document bundles. **success-stories** is an image flipbook (WebP q80 pages since Jul 2026; `Flipbook.tsx` loads a ±4 window around the current spread, so a first view fetches ~5 images; manifest imported at build time via `src/features/flipbooks/manifests.ts`). **catalog** holds only `petromac-product-catalog.pdf` — compressed to <4 MB on ingest (Ghostscript) and linearized (qpdf) — the download/email artifact; the browsing surface is the HTML catalog (see Architecture). The pdf.js viewer and its `search-index.json` were retired Jul 2026. Success-stories keeps its own compressed `email.pdf` (committed) since its source stays full-res for page rendering; it is ~6 MB at 200 dpi and is what the Download/Email buttons AND the filtered extracts are built from. Compression is an explicit DPI ladder in `compress_pdf()`, NOT gs's `/ebook`//`/screen` presets — those shipped 72 dpi silently for months (Aug 2026, docs/DECISIONS.md).
- `public/models/` + `public/draco/` — Draco-compressed GLBs (221 MB → 14 MB, Jul 2026) and the self-hosted decoder. `useGLTF(url, '/draco/')` everywhere — NEVER drop the second arg or drei falls back to a Google CDN and breaks the offline kiosk.
- `public/videos/` — `originals/` holds full-res masters (gitignored, too large for git); `transcoded/` holds the web-ready clips the site references (committed; the four homepage `*-subtitled` cuts are 1080p CRF 22 / 2.5 Mbps cap since Aug 2026 — WirelineExpress-subtitled is silent at every generation, so the homepage High Deviations tab plays WirelineExpress.mp4 — the full master cut with narration); `hero/` holds the homepage hero loop + its poster frame (committed; filenames are load-bearing — Hero.tsx references them directly). `kiosk-hd/` is GONE (Aug 2026) — the kiosk plays the same `transcoded/` files as the site; see docs/ADMIN.md §7.
- `sources/` — Content-pipeline drop zone: drop a file into `sources/{operations,catalog,success-stories}/` and run `pnpm run data` (dropped files gitignored; see `sources/README.md`)
- `src/data/` — Small typed TS modules only (e.g. `team.ts`)

## Key Conventions

- Environment variables for all secrets (see `.env.example`)
- Content updates (operations xlsx, catalog/success-stories PDFs) are dropped into the `sources/` folders — no env vars, no renaming; run `pnpm run data` (see `sources/README.md`)
- Flipbook tags CSV is the single source of truth for success stories filtering
- Kiosk service worker (`public/kiosk-sw.js`) scoped to `/intranet/kiosk/` only
- Security: rate limiting, HTML escaping, origin validation (URL hostname parsing), email allowlists
- Security headers configured in `next.config.ts` (HSTS, X-Frame-Options, etc.)

## Documentation

All docs live in `docs/`:

- [docs/ADMIN.md](docs/ADMIN.md) — Recurring content updates (operations data, flipbooks, patents, publications, team)
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — Architecture overview
- [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) — Development workflow & data conventions
- [docs/REPO_STRUCTURE.md](docs/REPO_STRUCTURE.md) — Full directory tree
- [docs/FLIPBOOKS.md](docs/FLIPBOOKS.md) — Flipbook build pipeline & troubleshooting
- [docs/KIOSK.md](docs/KIOSK.md) — Kiosk operations & offline caching
- [docs/TAILWIND_THEME.md](docs/TAILWIND_THEME.md) — Brand colors & typography
- [docs/EMAIL_SETUP.md](docs/EMAIL_SETUP.md) — Email (Microsoft Graph) configuration
- [docs/DNS.md](docs/DNS.md) — DNS zone (Cloudflare = single source of truth), history & cleanup questions
- [docs/VOCABULARY_MAP.md](docs/VOCABULARY_MAP.md) — SEO vocabulary, spelling policy & the signed-off page titles
- [docs/DECISIONS.md](docs/DECISIONS.md) — Decision log: why things are the way they are

## Current Tasks

See [TODO.md](TODO.md) for outstanding work items.
