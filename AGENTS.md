# Petromac Website — agent instructions

**See [CLAUDE.md](CLAUDE.md). It is the single source of truth for this repo.**

This file used to be a second copy of that content and silently went stale: it
still described a pdf.js catalog viewer, a public success-stories flipbook, SMTP
email and "push deploys production" long after all four had changed. Two
documents describing one codebase means one of them is wrong and nobody knows
which, so this is now a pointer instead of a copy.

Read `CLAUDE.md` for quick-reference commands, the commit/push hooks, the deploy
model (a push goes to TEST; production needs the Promote workflow), the
architecture, and where content lives. Deeper docs are indexed in
[docs/README.md](docs/README.md).
