services:
  petromac:
    image: ${IMAGE_REF:?IMAGE_REF is required}
    container_name: petromac
    env_file:
      - /opt/petromac/.env.prod
    networks:
      - web
    restart: unless-stopped

networks:
  web:
    external: true
